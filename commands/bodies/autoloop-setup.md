---
name: autoloop-setup
description: Retired in v3.0.0 - now an alias. Setup is now inline in the EVOLVE workflow when the program file is missing. Run /explore and pick option 7 (EVOLVE).
allowed-tools: Bash
---

Display:

  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  /autoloop-setup is no longer a separate
  command. The 4-question setup runs
  automatically inside the EVOLVE workflow
  the first time you launch it.

  Run /explore and pick option 7 (EVOLVE).
  If your program file does not exist yet
  EVOLVE will set it up before starting.

  This command is kept as an alias so
  muscle memory still works.
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stop here. Do not run any other logic.
