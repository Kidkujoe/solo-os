---
name: compass-project
description: Retired in v3.0.0 - now an alias. New-project market validation is part of the BUILD workflow's first step. Run /explore and pick option 4 (BUILD).
allowed-tools: Bash
---

Display:

  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  /compass-project is now part of the BUILD
  workflow.

  Run /explore and pick option 4 (BUILD).
  Step 1 (Validate and Plan) runs the same
  market validation, then continues into
  strategy, stack and scaffold.

  If you want validation only, you can
  also stop after Step 1.

  This command is kept as an alias so
  muscle memory still works, but the new
  way is /explore.
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stop here. Do not run any other logic.
