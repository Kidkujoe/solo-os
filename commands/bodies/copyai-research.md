---
name: copyai-research
description: Retired in v3.0.0 - now an alias. Research-only copy intelligence is part of the RESEARCH workflow. Run /explore and pick option 6 (RESEARCH) or use /copyai --research-only.
allowed-tools: Bash
---

Display:

  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  /copyai-research is now part of the
  RESEARCH workflow (and stays available
  via /copyai --research-only).

  Run /explore and pick option 6 (RESEARCH)
  to ingest customer-language sources into
  the wiki, where /copyai will then read
  them automatically on its next run.

  Or run /copyai --research-only directly
  for the legacy research-only behaviour.

  This command is kept as an alias so
  muscle memory still works, but the new
  way is /explore.
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stop here. Do not run any other logic.
