---
name: screenshots
description: Retired in v3.0.0 - now an alias. Screenshot management lives under /report (use /report --screenshots).
allowed-tools: Bash
---

Display:

  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  /screenshots is now part of /report.

  Run /report --screenshots to manage
  screenshots from any test session.

  This command is kept as an alias so
  muscle memory still works, but the new
  way is /report.
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stop here. Do not run any other logic.
