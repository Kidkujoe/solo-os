---
name: test-quick
description: Retired in v3.0.0 - now an alias. Quick visual checks are part of the SHIP workflow's drift mode. Run /explore and pick option 1 (SHIP).
allowed-tools: Bash
---

Display:

  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  /test-quick is now part of the SHIP workflow.

  Run /explore and pick option 1 (SHIP).
  Drift-check mode (option B in SHIP) is
  the new equivalent — it audits only what
  changed since the last review.

  This command is kept as an alias so
  muscle memory still works, but the new
  way is /explore.
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stop here. Do not run any other logic.
